node_modules
dist
dev.log
*.local
.DS_Store

# Freebuff workspace metadata
.freebuff/

# Dev server output
.dev-server.log

# Convex log tails
.convex-logs.txt

# NOTE: convex/_generated is committed (not ignored) because Vercel builds
# run `tsc -b` remotely and CLI deploys exclude gitignored files.

# Recovered production bundles from the original deployment (reference only)
_reference/
